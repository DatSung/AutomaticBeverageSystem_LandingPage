---
import { BRAND } from "@constants/brand";
---

<section
  class="min-h-screen bg-gradient-to-br from-cyan-50 via-blue-50 to-cyan-100 py-12 px-4"
>
  <!-- Background decorative elements -->
  <div class="absolute inset-0 opacity-30">
    <div
      class="absolute top-20 left-10 w-32 h-32 bg-gradient-to-br from-cyan-400 to-blue-500 rounded-full blur-3xl"
    >
    </div>
    <div
      class="absolute top-1/2 right-20 w-40 h-40 bg-gradient-to-br from-blue-400 to-cyan-500 rounded-full blur-3xl"
    >
    </div>
    <div
      class="absolute bottom-32 left-1/3 w-36 h-36 bg-gradient-to-br from-cyan-300 to-blue-400 rounded-full blur-3xl"
    >
    </div>
  </div>

  <div class="relative z-10 max-w-5xl mx-auto">
    <!-- Header -->
    <div class="text-center mb-16">
      <div
        class="inline-flex items-center justify-center w-20 h-20 bg-gradient-to-r from-cyan-500 to-blue-600 rounded-2xl mb-8 shadow-2xl transform hover:scale-110 transition-all duration-300"
      >
        <svg
          class="w-10 h-10 text-white"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"
          ></path>
        </svg>
      </div>
      <h1
        class="text-5xl md:text-6xl font-extrabold bg-gradient-to-r from-cyan-600 via-blue-600 to-cyan-700 bg-clip-text text-transparent mb-6"
      >
        Liên hệ hợp tác
      </h1>
      <div
        class="w-24 h-1 bg-gradient-to-r from-cyan-500 to-blue-600 mx-auto mb-8 rounded-full"
      >
      </div>
      <p class="text-xl text-gray-700 max-w-2xl mx-auto font-medium">
        Hãy để lại thông tin chi tiết để chúng tôi có thể tư vấn và hỗ trợ bạn
        một cách tốt nhất.
      </p>
    </div>

    <div class="flex flex-col lg:flex-row gap-12">
      <!-- Contact Info Sidebar -->
      <div class="lg:col-span-1 space-y-8">
        <div
          class="bg-white/90 backdrop-blur-lg rounded-3xl p-8 shadow-2xl border border-cyan-100/50 hover:shadow-3xl transition-all duration-500"
        >
          <h3 class="text-2xl font-bold text-gray-800 mb-6 flex items-center">
            <div
              class="w-10 h-10 bg-gradient-to-r from-cyan-500 to-blue-600 rounded-xl flex items-center justify-center mr-4"
            >
              <svg
                class="w-5 h-5 text-white"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"
                ></path>
              </svg>
            </div>
            Thông tin liên hệ
          </h3>
          <div class="space-y-5">
            <div
              class="flex items-center p-4 hover:bg-cyan-50 rounded-xl transition-colors"
            >
              <div
                class="w-12 h-12 bg-cyan-100 rounded-xl flex items-center justify-center mr-4"
              >
                <svg
                  class="w-6 h-6 text-cyan-600"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"
                  ></path>
                </svg>
              </div>
              <div>
                <p class="text-sm text-gray-500 font-medium">Email</p>
                <p class="text-gray-800 font-semibold">{BRAND.email}</p>
              </div>
            </div>
            <div
              class="flex items-center p-4 hover:bg-cyan-50 rounded-xl transition-colors"
            >
              <div
                class="w-12 h-12 bg-cyan-100 rounded-xl flex items-center justify-center mr-4"
              >
                <svg
                  class="w-6 h-6 text-cyan-600"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"
                  ></path>
                </svg>
              </div>
              <div>
                <p class="text-sm text-gray-500 font-medium">Điện thoại</p>
                <p class="text-gray-800 font-semibold">{BRAND.contactPhone}</p>
              </div>
            </div>
            <div
              class="flex items-center p-4 hover:bg-cyan-50 rounded-xl transition-colors"
            >
              <div
                class="w-12 h-12 bg-cyan-100 rounded-xl flex items-center justify-center mr-4"
              >
                <svg
                  class="w-6 h-6 text-cyan-600"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
              </div>
              <div>
                <p class="text-sm text-gray-500 font-medium">Giờ làm việc</p>
                <p class="text-gray-800 font-semibold">{BRAND.time}</p>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Main Form -->
      <div class="lg:col-span-2">
        <form
          id="contactForm"
          class="bg-white/90 backdrop-blur-lg rounded-3xl p-10 shadow-2xl border border-cyan-100/50 hover:shadow-3xl transition-all duration-500"
        >
          <div class="text-center mb-8">
            <h2 class="text-3xl font-bold text-gray-800 mb-2">
              Gửi yêu cầu hợp tác
            </h2>
            <p class="text-gray-600">
              Điền thông tin để chúng tôi hỗ trợ bạn tốt nhất
            </p>
          </div>

          <div class="grid md:grid-cols-2 gap-6 mb-6">
            <!-- Name Input -->
            <div class="space-y-2">
              <label class="block text-sm font-semibold text-gray-700">
                Tên của bạn <span class="text-red-500">*</span>
              </label>
              <div class="relative">
                <input
                  name="name"
                  required
                  placeholder="Nhập tên đầy đủ"
                  class="w-full px-4 py-4 border-2 rounded-2xl border-gray-200 focus:border-cyan-500 hover:border-cyan-300 focus:outline-none transition-all duration-300 text-gray-800 font-medium bg-white/80 backdrop-blur-sm pr-12"
                />
                <svg
                  class="absolute right-4 top-1/2 transform -translate-y-1/2 w-5 h-5 text-gray-400"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"
                  ></path>
                </svg>
              </div>
              <span
                class="text-red-500 text-sm hidden error-msg"
                data-field="name"></span>
            </div>

            <!-- Email Input -->
            <div class="space-y-2">
              <label class="block text-sm font-semibold text-gray-700">
                Email <span class="text-red-500">*</span>
              </label>
              <div class="relative">
                <input
                  name="email"
                  type="email"
                  required
                  placeholder="example@email.com"
                  class="w-full px-4 py-4 border-2 rounded-2xl border-gray-200 focus:border-cyan-500 hover:border-cyan-300 focus:outline-none transition-all duration-300 text-gray-800 font-medium bg-white/80 backdrop-blur-sm pr-12"
                />
                <svg
                  class="absolute right-4 top-1/2 transform -translate-y-1/2 w-5 h-5 text-gray-400"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"
                  ></path>
                </svg>
              </div>
              <span
                class="text-red-500 text-sm hidden error-msg"
                data-field="email"></span>
            </div>

            <!-- Phone Input -->
            <div class="space-y-2">
              <label class="block text-sm font-semibold text-gray-700">
                Số điện thoại <span class="text-red-500">*</span>
              </label>
              <div class="relative">
                <input
                  name="phone"
                  required
                  placeholder="0123 456 789"
                  class="w-full px-4 py-4 border-2 rounded-2xl border-gray-200 focus:border-cyan-500 hover:border-cyan-300 focus:outline-none transition-all duration-300 text-gray-800 font-medium bg-white/80 backdrop-blur-sm pr-12"
                />
                <svg
                  class="absolute right-4 top-1/2 transform -translate-y-1/2 w-5 h-5 text-gray-400"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"
                  ></path>
                </svg>
              </div>
              <span
                class="text-red-500 text-sm hidden error-msg"
                data-field="phone"></span>
            </div>

            <!-- Time Select -->
            <div class="space-y-2">
              <label class="block text-sm font-semibold text-gray-700">
                Khung giờ tư vấn
              </label>
              <div class="relative">
                <select
                  name="time"
                  class="w-full px-4 py-4 border-2 rounded-2xl border-gray-200 focus:border-cyan-500 hover:border-cyan-300 focus:outline-none transition-all duration-300 text-gray-800 font-medium bg-white/80 backdrop-blur-sm appearance-none pr-12"
                >
                  <option value="">Chọn khung giờ</option>
                  <option value="8:00 - 10:00">8:00 - 10:00</option>
                  <option value="10:00 - 12:00">10:00 - 12:00</option>
                  <option value="14:00 - 16:00">14:00 - 16:00</option>
                  <option value="16:00 - 18:00">16:00 - 18:00</option>
                </select>
                <svg
                  class="absolute right-4 top-1/2 transform -translate-y-1/2 w-5 h-5 text-gray-400 pointer-events-none"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M19 9l-7 7-7-7"></path>
                </svg>
              </div>
            </div>
          </div>

          <!-- Content Textarea -->
          <div class="space-y-2 mb-6">
            <label class="block text-sm font-semibold text-gray-700">
              Nội dung chi tiết
            </label>
            <div class="relative">
              <textarea
                name="content"
                rows="5"
                placeholder="Mô tả chi tiết về nhu cầu hợp tác, dự án hoặc câu hỏi của bạn..."
                class="w-full px-4 py-4 border-2 border-gray-200 rounded-2xl focus:border-cyan-500 hover:border-cyan-300 focus:outline-none transition-all duration-300 resize-none text-gray-800 font-medium bg-white/80 backdrop-blur-sm"
              ></textarea>
              <div class="absolute bottom-3 right-3 text-xs text-gray-400">
                <span id="charCount">0</span> ký tự
              </div>
            </div>
          </div>

          <!-- Error Box -->
          <div id="formError" class="mb-6"></div>

          <!-- Action Buttons -->
          <div
            class="flex flex-col sm:flex-row gap-4 pt-6 border-t border-gray-100"
          >
            <button
              type="submit"
              id="submitBtn"
              class="flex-1 bg-gradient-to-r from-cyan-500 via-blue-600 to-cyan-600 hover:from-cyan-600 hover:via-blue-700 hover:to-cyan-700 text-white px-10 py-4 rounded-2xl font-bold transition-all duration-300 transform hover:scale-105 hover:shadow-2xl flex items-center justify-center space-x-3"
            >
              <svg
                class="w-5 h-5"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8"></path>
              </svg>
              <span>Gửi yêu cầu hợp tác</span>
            </button>
            <button
              type="reset"
              class="px-10 py-4 border-2 border-gray-300 text-gray-700 rounded-2xl font-bold hover:border-gray-400 hover:bg-gray-50 transition-all duration-300 hover:shadow-lg"
            >
              Đặt lại
            </button>
          </div>
        </form>
      </div>
    </div>
  </div>
</section>

<script type="module">
  
  const form = document.getElementById("contactForm");
  const submitBtn = document.getElementById("submitBtn");
  const errorBox = document.getElementById("formError");
  const textarea = document.querySelector('textarea[name="content"]');
  const charCount = document.getElementById("charCount");

  // Character counter
  if (textarea && charCount) {
    textarea.addEventListener("input", function () {
      charCount.textContent = this.value.length;
    });
  }

  // Validation functions
  const validateEmail = (email) => /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email);
  const validatePhone = (phone) => /^[0-9+\-\s()]{10,15}$/.test(phone);

  const showError = (field, message) => {
    const input = document.querySelector(`input[name="${field}"]`);
    const errorMsg = document.querySelector(
      `.error-msg[data-field="${field}"]`
    );

    if (input && errorMsg) {
      input.classList.remove("border-gray-200", "border-green-300");
      input.classList.add("border-red-300", "bg-red-50");
      errorMsg.textContent = message;
      errorMsg.classList.remove("hidden");
    }
  };

  const showSuccess = (field) => {
    const input = document.querySelector(`input[name="${field}"]`);
    const errorMsg = document.querySelector(
      `.error-msg[data-field="${field}"]`
    );

    if (input && errorMsg) {
      input.classList.remove("border-red-300", "bg-red-50");
      input.classList.add("border-green-300", "bg-green-50");
      errorMsg.classList.add("hidden");
    }
  };

  const clearErrors = () => {
    document
      .querySelectorAll(".error-msg")
      .forEach((msg) => msg.classList.add("hidden"));
    document.querySelectorAll("input").forEach((input) => {
      input.classList.remove(
        "border-red-300",
        "bg-red-50",
        "border-green-300",
        "bg-green-50"
      );
      input.classList.add("border-gray-200");
    });
  };

  const validateForm = (formData) => {
    let isValid = true;
    clearErrors();

    if (!formData.name.trim()) {
      showError("name", "Tên là bắt buộc");
      isValid = false;
    } else if (formData.name.trim().length < 2) {
      showError("name", "Tên phải có ít nhất 2 ký tự");
      isValid = false;
    } else {
      showSuccess("name");
    }

    if (!formData.email.trim()) {
      showError("email", "Email là bắt buộc");
      isValid = false;
    } else if (!validateEmail(formData.email)) {
      showError("email", "Email không hợp lệ");
      isValid = false;
    } else {
      showSuccess("email");
    }

    if (!formData.phone.trim()) {
      showError("phone", "Số điện thoại là bắt buộc");
      isValid = false;
    } else if (!validatePhone(formData.phone)) {
      showError("phone", "Số điện thoại không hợp lệ");
      isValid = false;
    } else {
      showSuccess("phone");
    }

    return isValid;
  };

  // Real-time validation
  document.querySelectorAll("input[required]").forEach((input) => {
    input.addEventListener("blur", function () {
      const formData = { [this.name]: this.value };
      validateForm(formData);
    });
  });

  form?.addEventListener("submit", async (e) => {
    e.preventDefault();
    errorBox.innerHTML = "";

    const formData = new FormData(form);
    const payload = Object.fromEntries(formData.entries());

    if (!validateForm(payload)) return;

    submitBtn.disabled = true;
    submitBtn.innerHTML = `
      <svg class="animate-spin w-5 h-5 mr-2" fill="none" viewBox="0 0 24 24">
        <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
        <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
      </svg>
      <span>Đang gửi...</span>
    `;

    try {
      const res = await fetch(
        "https://autobrew.funnydev.id.vn/api/v1/cooperate-request",
        {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify(payload),
        }
      );

      const result = await res.json();

      if (result.isSuccess) {
        form.innerHTML = `
        <div class="text-center py-16">
          <div class="inline-flex items-center justify-center w-20 h-20 bg-gradient-to-r from-green-400 to-green-600 rounded-full mb-8 shadow-2xl">
            <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M5 13l4 4L19 7"/>
            </svg>
          </div>
          <h3 class="text-4xl font-bold text-gray-800 mb-6">Gửi yêu cầu thành công!</h3>
          <p class="text-xl text-gray-600 mb-10 max-w-md mx-auto">
            Cảm ơn bạn đã liên hệ với chúng tôi. Chúng tôi sẽ phản hồi trong vòng 24 giờ tới.
          </p>
          <button 
            onclick="window.location.reload()" 
            class="bg-gradient-to-r from-cyan-500 to-blue-600 hover:from-cyan-600 hover:to-blue-700 text-white px-8 py-4 rounded-2xl font-bold transition-all duration-300 transform hover:scale-105 hover:shadow-xl"
          >
            Gửi yêu cầu khác
          </button>
        </div>`;
      } else {
        errorBox.innerHTML = `
          <div class="bg-red-50 border-2 border-red-200 text-red-800 px-6 py-4 rounded-2xl flex items-center shadow-lg">
            <svg class="w-6 h-6 mr-3 text-red-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
            </svg>
            <div>
              <p class="font-bold">Có lỗi xảy ra!</p>
              <p class="text-sm">${result.message}</p>
            </div>
          </div>`;
      }
    } catch (err) {
      errorBox.innerHTML = `
        <div class="bg-red-50 border-2 border-red-200 text-red-800 px-6 py-4 rounded-2xl flex items-center shadow-lg">
          <svg class="w-6 h-6 mr-3 text-red-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
          </svg>
          <div>
            <p class="font-bold">Không thể kết nối!</p>
            <p class="text-sm">Vui lòng kiểm tra kết nối và thử lại.</p>
          </div>
        </div>`;
    } finally {
      submitBtn.disabled = false;
      submitBtn.innerHTML = `
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8"/>
        </svg>
        <span>Gửi yêu cầu hợp tác</span>
      `;
    }
  });
</script>
